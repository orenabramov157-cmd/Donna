{
  "name": "accountability-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Single-user accountability drill sergeant: iMessage nags via LoopMessage, Trello-synced task list, Cloudflare Worker + D1, $0/mo.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.27.0"
  }
}
